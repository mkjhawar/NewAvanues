// Code generated by Wire protocol buffer compiler, do not edit.
// Source: com.augmentalis.rpc.ava.ChatRequest in ava.proto
@file:Suppress(
  "DEPRECATION",
  "RUNTIME_ANNOTATION_NOT_SUPPORTED",
)

package com.augmentalis.rpc.ava

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.redactElements
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.Suppress
import kotlin.collections.List
import okio.ByteString

/**
 * Chat request
 */
public class ChatRequest(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "requestId",
    schemaIndex = 0,
  )
  public val request_id: String = "",
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 1,
  )
  public val message: String = "",
  history: List<ChatMessage> = emptyList(),
  @field:WireField(
    tag = 4,
    adapter = "com.augmentalis.rpc.ava.ChatConfig#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 3,
  )
  public val config: ChatConfig? = null,
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<ChatRequest, Nothing>(ADAPTER, unknownFields) {
  @field:WireField(
    tag = 3,
    adapter = "com.augmentalis.rpc.ava.ChatMessage#ADAPTER",
    label = WireField.Label.REPEATED,
    schemaIndex = 2,
  )
  public val history: List<ChatMessage> = immutableCopyOf("history", history)

  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN,
  )
  override fun newBuilder(): Nothing = throw
      AssertionError("Builders are deprecated and only available in a javaInterop build; see https://square.github.io/wire/wire_compiler/#kotlin")

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is ChatRequest) return false
    if (unknownFields != other.unknownFields) return false
    if (request_id != other.request_id) return false
    if (message != other.message) return false
    if (history != other.history) return false
    if (config != other.config) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + request_id.hashCode()
      result = result * 37 + message.hashCode()
      result = result * 37 + history.hashCode()
      result = result * 37 + (config?.hashCode() ?: 0)
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """request_id=${sanitize(request_id)}"""
    result += """message=${sanitize(message)}"""
    if (history.isNotEmpty()) result += """history=$history"""
    if (config != null) result += """config=$config"""
    return result.joinToString(prefix = "ChatRequest{", separator = ", ", postfix = "}")
  }

  public fun copy(
    request_id: String = this.request_id,
    message: String = this.message,
    history: List<ChatMessage> = this.history,
    config: ChatConfig? = this.config,
    unknownFields: ByteString = this.unknownFields,
  ): ChatRequest = ChatRequest(request_id, message, history, config, unknownFields)

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<ChatRequest> = object : ProtoAdapter<ChatRequest>(
      FieldEncoding.LENGTH_DELIMITED, 
      ChatRequest::class, 
      "type.googleapis.com/com.augmentalis.rpc.ava.ChatRequest", 
      PROTO_3, 
      null, 
      "ava.proto"
    ) {
      override fun encodedSize(`value`: ChatRequest): Int {
        var size = value.unknownFields.size
        if (value.request_id != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(1, value.request_id)
        }
        if (value.message != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(2, value.message)
        }
        size += ChatMessage.ADAPTER.asRepeated().encodedSizeWithTag(3, value.history)
        if (value.config != null) {
          size += ChatConfig.ADAPTER.encodedSizeWithTag(4, value.config)
        }
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: ChatRequest) {
        if (value.request_id != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 1, value.request_id)
        }
        if (value.message != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 2, value.message)
        }
        ChatMessage.ADAPTER.asRepeated().encodeWithTag(writer, 3, value.history)
        if (value.config != null) {
          ChatConfig.ADAPTER.encodeWithTag(writer, 4, value.config)
        }
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: ChatRequest) {
        writer.writeBytes(value.unknownFields)
        if (value.config != null) {
          ChatConfig.ADAPTER.encodeWithTag(writer, 4, value.config)
        }
        ChatMessage.ADAPTER.asRepeated().encodeWithTag(writer, 3, value.history)
        if (value.message != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 2, value.message)
        }
        if (value.request_id != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 1, value.request_id)
        }
      }

      override fun decode(reader: ProtoReader): ChatRequest {
        var request_id: String = ""
        var message: String = ""
        val history = mutableListOf<ChatMessage>()
        var config: ChatConfig? = null
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> request_id = ProtoAdapter.STRING.decode(reader)
            2 -> message = ProtoAdapter.STRING.decode(reader)
            3 -> history.add(ChatMessage.ADAPTER.decode(reader))
            4 -> config = ChatConfig.ADAPTER.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return ChatRequest(
          request_id = request_id,
          message = message,
          history = history,
          config = config,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: ChatRequest): ChatRequest = value.copy(
        history = value.history.redactElements(ChatMessage.ADAPTER),
        config = value.config?.let(ChatConfig.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
