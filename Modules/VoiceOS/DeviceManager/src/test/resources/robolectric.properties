# Robolectric configuration for DeviceManager tests
sdk=30
# Disable strict mode to allow test execution without full manifest
enabledSdks=30
# Suppress manifest warnings
disableManifestRegistration=true
