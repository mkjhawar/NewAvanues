<?xml version="1.0" encoding="utf-8"?>
<!--
    bg_button_primary.xml - Primary button background with ripple effect

    Used for primary action buttons (e.g., "Yes" in consent dialog)
    Provides Material-like appearance without Compose dependency

    Created: 2025-12-08
    Purpose: Fix consent dialog Compose lifecycle crash
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorPrimary"/>
            <corners android:radius="4dp"/>
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorPrimary"/>
            <corners android:radius="4dp"/>
        </shape>
    </item>
</ripple>
