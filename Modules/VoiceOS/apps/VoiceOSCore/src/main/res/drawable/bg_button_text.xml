<?xml version="1.0" encoding="utf-8"?>
<!--
    bg_button_text.xml - Text button background with ripple effect

    Used for secondary action buttons (e.g., "No", "Skip" in consent dialog)
    Transparent background with ripple on press

    Created: 2025-12-08
    Purpose: Fix consent dialog Compose lifecycle crash
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white"/>
            <corners android:radius="4dp"/>
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent"/>
            <corners android:radius="4dp"/>
        </shape>
    </item>
</ripple>
