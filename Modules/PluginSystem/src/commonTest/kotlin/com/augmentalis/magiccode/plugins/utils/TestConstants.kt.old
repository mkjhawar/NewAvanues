package com.augmentalis.magiccode.plugins.utils

/**
 * Test constants shared across test suites.
 */
object TestConstants {
    // Test plugin IDs
    const val TEST_PLUGIN_ID = "com.test.sample-plugin"
    const val TEST_PLUGIN_ID_2 = "com.test.another-plugin"
    const val TEST_PLUGIN_ID_DEPENDENCY = "com.test.dependency-plugin"

    // Test plugin names
    const val TEST_PLUGIN_NAME = "Sample Plugin"
    const val TEST_PLUGIN_VERSION = "1.0.0"
    const val TEST_PLUGIN_AUTHOR = "Test Author"

    // Test paths
    const val TEST_MANIFEST_PATH = "/test/plugins/sample-plugin/plugin.yaml"
    const val TEST_LIBRARY_PATH = "/test/plugins/sample-plugin/lib/plugin.jar"
    const val TEST_APP_DATA_DIR = "/test/app-data"
    const val TEST_PLUGIN_ROOT = "/test/plugins/sample-plugin"
    const val TEST_BACKUP_DIR = "/test/backups"

    // Test entrypoint
    const val TEST_ENTRYPOINT = "com.test.SamplePlugin"

    // Test URIs
    const val TEST_ASSET_URI = "plugin://com.test.sample-plugin/themes/dark-theme.yaml"
    const val TEST_ASSET_URI_IMAGE = "plugin://com.test.sample-plugin/images/icon.png"
    const val TEST_ASSET_URI_FONT = "plugin://com.test.sample-plugin/fonts/custom-font.ttf"

    // Test file paths
    const val TEST_ASSET_PATH = "/test/plugins/sample-plugin/themes/dark-theme.yaml"
    const val TEST_ASSET_IMAGE_PATH = "/test/plugins/sample-plugin/assets/images/icon.png"

    // Test checksums (SHA256 hex strings)
    const val TEST_CHECKSUM_SHA256 = "a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2"
    const val TEST_CHECKSUM_MD5 = "1234567890abcdef1234567890abcdef"

    // Test theme colors
    const val TEST_COLOR_PRIMARY = "#007AFF"
    const val TEST_COLOR_SECONDARY = "#5856D6"
    const val TEST_COLOR_BACKGROUND = "#1C1C1E"
    const val TEST_COLOR_TEXT = "#FFFFFF"

    // Test font sizes
    const val TEST_FONT_SIZE_SMALL = 14
    const val TEST_FONT_SIZE_MEDIUM = 16
    const val TEST_FONT_SIZE_LARGE = 20

    // Test spacing values
    const val TEST_SPACING_XS = 4
    const val TEST_SPACING_SM = 8
    const val TEST_SPACING_MD = 16
    const val TEST_SPACING_LG = 24
    const val TEST_SPACING_XL = 32

    // Test semver versions
    const val SEMVER_1_0_0 = "1.0.0"
    const val SEMVER_1_2_3 = "1.2.3"
    const val SEMVER_1_5_0 = "1.5.0"
    const val SEMVER_2_0_0 = "2.0.0"
    const val SEMVER_2_3_0 = "2.3.0"
    const val SEMVER_2_3_1 = "2.3.1"

    // Test semver constraints
    const val CONSTRAINT_CARET_1_0_0 = "^1.0.0"
    const val CONSTRAINT_TILDE_2_3_0 = "~2.3.0"
    const val CONSTRAINT_RANGE = ">=1.5.0 <2.0.0"
    const val CONSTRAINT_WILDCARD = "1.*.0"

    // Cache settings
    const val TEST_CACHE_CAPACITY = 10

    // File sizes
    const val TEST_FILE_SIZE_SMALL = 1024L // 1 KB
    const val TEST_FILE_SIZE_MEDIUM = 1024L * 100 // 100 KB
    const val TEST_FILE_SIZE_LARGE = 1024L * 1024 * 10 // 10 MB

    // Timestamps
    const val TEST_TIMESTAMP = 1698364800000L // 2023-10-27 00:00:00 UTC

    // Test content
    const val TEST_MANIFEST_YAML = """
id: com.test.sample-plugin
name: Sample Plugin
version: 1.0.0
author: Test Author
description: A sample plugin for testing
entrypoint: com.test.SamplePlugin
capabilities:
  - themes
  - ui_components
dependencies: []
permissions:
  - NETWORK
  - STORAGE_READ
source: THIRD_PARTY
verificationLevel: UNVERIFIED
assets:
  themes:
    - dark-theme.yaml
  images:
    - icon.png
"""

    const val TEST_THEME_YAML = """
name: Dark Theme
version: 1.0.0
description: A dark color scheme
author: Test Author
colors:
  primary: "#007AFF"
  secondary: "#5856D6"
  background: "#1C1C1E"
  text: "#FFFFFF"
typography:
  fontFamily: "SF Pro"
  fontSize:
    small: 14
    medium: 16
    large: 20
  fontWeight:
    regular: 400
    bold: 700
spacing:
  xs: 4
  sm: 8
  md: 16
  lg: 24
  xl: 32
"""
}
