// Code generated by Wire protocol buffer compiler, do not edit.
// Source: com.augmentalis.universalrpc.plugin.PluginInfo in plugin.proto
@file:Suppress(
  "DEPRECATION",
  "RUNTIME_ANNOTATION_NOT_SUPPORTED",
)

package com.augmentalis.universalrpc.plugin

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.redactElements
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.Suppress
import kotlin.collections.List
import okio.ByteString

/**
 * Plugin information
 */
public class PluginInfo(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "pluginId",
    schemaIndex = 0,
  )
  public val plugin_id: String = "",
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "pluginName",
    schemaIndex = 1,
  )
  public val plugin_name: String = "",
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 2,
  )
  public val version: String = "",
  @field:WireField(
    tag = 4,
    adapter = "com.augmentalis.universalrpc.plugin.PluginStateProto#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 3,
  )
  public val state: PluginStateProto = PluginStateProto.PLUGIN_STATE_UNKNOWN,
  capabilities: List<PluginCapabilityProto> = emptyList(),
  @field:WireField(
    tag = 6,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "endpointAddress",
    schemaIndex = 5,
  )
  public val endpoint_address: String = "",
  @field:WireField(
    tag = 7,
    adapter = "com.squareup.wire.ProtoAdapter#INT64",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "registeredAt",
    schemaIndex = 6,
  )
  public val registered_at: Long = 0L,
  @field:WireField(
    tag = 8,
    adapter = "com.squareup.wire.ProtoAdapter#INT64",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "lastHealthCheck",
    schemaIndex = 7,
  )
  public val last_health_check: Long = 0L,
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<PluginInfo, Nothing>(ADAPTER, unknownFields) {
  @field:WireField(
    tag = 5,
    adapter = "com.augmentalis.universalrpc.plugin.PluginCapabilityProto#ADAPTER",
    label = WireField.Label.REPEATED,
    schemaIndex = 4,
  )
  public val capabilities: List<PluginCapabilityProto> = immutableCopyOf("capabilities", capabilities)

  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN,
  )
  override fun newBuilder(): Nothing = throw
      AssertionError("Builders are deprecated and only available in a javaInterop build; see https://square.github.io/wire/wire_compiler/#kotlin")

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is PluginInfo) return false
    if (unknownFields != other.unknownFields) return false
    if (plugin_id != other.plugin_id) return false
    if (plugin_name != other.plugin_name) return false
    if (version != other.version) return false
    if (state != other.state) return false
    if (capabilities != other.capabilities) return false
    if (endpoint_address != other.endpoint_address) return false
    if (registered_at != other.registered_at) return false
    if (last_health_check != other.last_health_check) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + plugin_id.hashCode()
      result = result * 37 + plugin_name.hashCode()
      result = result * 37 + version.hashCode()
      result = result * 37 + state.hashCode()
      result = result * 37 + capabilities.hashCode()
      result = result * 37 + endpoint_address.hashCode()
      result = result * 37 + registered_at.hashCode()
      result = result * 37 + last_health_check.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """plugin_id=${sanitize(plugin_id)}"""
    result += """plugin_name=${sanitize(plugin_name)}"""
    result += """version=${sanitize(version)}"""
    result += """state=$state"""
    if (capabilities.isNotEmpty()) result += """capabilities=$capabilities"""
    result += """endpoint_address=${sanitize(endpoint_address)}"""
    result += """registered_at=$registered_at"""
    result += """last_health_check=$last_health_check"""
    return result.joinToString(prefix = "PluginInfo{", separator = ", ", postfix = "}")
  }

  public fun copy(
    plugin_id: String = this.plugin_id,
    plugin_name: String = this.plugin_name,
    version: String = this.version,
    state: PluginStateProto = this.state,
    capabilities: List<PluginCapabilityProto> = this.capabilities,
    endpoint_address: String = this.endpoint_address,
    registered_at: Long = this.registered_at,
    last_health_check: Long = this.last_health_check,
    unknownFields: ByteString = this.unknownFields,
  ): PluginInfo = PluginInfo(plugin_id, plugin_name, version, state, capabilities, endpoint_address, registered_at, last_health_check, unknownFields)

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<PluginInfo> = object : ProtoAdapter<PluginInfo>(
      FieldEncoding.LENGTH_DELIMITED,
      PluginInfo::class,
      "type.googleapis.com/com.augmentalis.universalrpc.plugin.PluginInfo",
      PROTO_3,
      null,
      "plugin.proto"
    ) {
      override fun encodedSize(`value`: PluginInfo): Int {
        var size = value.unknownFields.size
        if (value.plugin_id != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(1, value.plugin_id)
        }
        if (value.plugin_name != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(2, value.plugin_name)
        }
        if (value.version != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(3, value.version)
        }
        if (value.state != PluginStateProto.PLUGIN_STATE_UNKNOWN) {
          size += PluginStateProto.ADAPTER.encodedSizeWithTag(4, value.state)
        }
        size += PluginCapabilityProto.ADAPTER.asRepeated().encodedSizeWithTag(5, value.capabilities)
        if (value.endpoint_address != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(6, value.endpoint_address)
        }
        if (value.registered_at != 0L) {
          size += ProtoAdapter.INT64.encodedSizeWithTag(7, value.registered_at)
        }
        if (value.last_health_check != 0L) {
          size += ProtoAdapter.INT64.encodedSizeWithTag(8, value.last_health_check)
        }
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: PluginInfo) {
        if (value.plugin_id != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 1, value.plugin_id)
        }
        if (value.plugin_name != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 2, value.plugin_name)
        }
        if (value.version != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 3, value.version)
        }
        if (value.state != PluginStateProto.PLUGIN_STATE_UNKNOWN) {
          PluginStateProto.ADAPTER.encodeWithTag(writer, 4, value.state)
        }
        PluginCapabilityProto.ADAPTER.asRepeated().encodeWithTag(writer, 5, value.capabilities)
        if (value.endpoint_address != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 6, value.endpoint_address)
        }
        if (value.registered_at != 0L) {
          ProtoAdapter.INT64.encodeWithTag(writer, 7, value.registered_at)
        }
        if (value.last_health_check != 0L) {
          ProtoAdapter.INT64.encodeWithTag(writer, 8, value.last_health_check)
        }
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: PluginInfo) {
        writer.writeBytes(value.unknownFields)
        if (value.last_health_check != 0L) {
          ProtoAdapter.INT64.encodeWithTag(writer, 8, value.last_health_check)
        }
        if (value.registered_at != 0L) {
          ProtoAdapter.INT64.encodeWithTag(writer, 7, value.registered_at)
        }
        if (value.endpoint_address != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 6, value.endpoint_address)
        }
        PluginCapabilityProto.ADAPTER.asRepeated().encodeWithTag(writer, 5, value.capabilities)
        if (value.state != PluginStateProto.PLUGIN_STATE_UNKNOWN) {
          PluginStateProto.ADAPTER.encodeWithTag(writer, 4, value.state)
        }
        if (value.version != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 3, value.version)
        }
        if (value.plugin_name != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 2, value.plugin_name)
        }
        if (value.plugin_id != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 1, value.plugin_id)
        }
      }

      override fun decode(reader: ProtoReader): PluginInfo {
        var plugin_id: String = ""
        var plugin_name: String = ""
        var version: String = ""
        var state: PluginStateProto = PluginStateProto.PLUGIN_STATE_UNKNOWN
        val capabilities = mutableListOf<PluginCapabilityProto>()
        var endpoint_address: String = ""
        var registered_at: Long = 0L
        var last_health_check: Long = 0L
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> plugin_id = ProtoAdapter.STRING.decode(reader)
            2 -> plugin_name = ProtoAdapter.STRING.decode(reader)
            3 -> version = ProtoAdapter.STRING.decode(reader)
            4 -> try {
              state = PluginStateProto.ADAPTER.decode(reader)
            } catch (e: ProtoAdapter.EnumConstantNotFoundException) {
              reader.addUnknownField(tag, FieldEncoding.VARINT, e.value.toLong())
            }
            5 -> capabilities.add(PluginCapabilityProto.ADAPTER.decode(reader))
            6 -> endpoint_address = ProtoAdapter.STRING.decode(reader)
            7 -> registered_at = ProtoAdapter.INT64.decode(reader)
            8 -> last_health_check = ProtoAdapter.INT64.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return PluginInfo(
          plugin_id = plugin_id,
          plugin_name = plugin_name,
          version = version,
          state = state,
          capabilities = capabilities,
          endpoint_address = endpoint_address,
          registered_at = registered_at,
          last_health_check = last_health_check,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: PluginInfo): PluginInfo = value.copy(
        capabilities = value.capabilities.redactElements(PluginCapabilityProto.ADAPTER),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
