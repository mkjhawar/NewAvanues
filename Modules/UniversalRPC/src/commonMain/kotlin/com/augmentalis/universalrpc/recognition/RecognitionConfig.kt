// Code generated by Wire protocol buffer compiler, do not edit.
// Source: com.augmentalis.universalrpc.recognition.RecognitionConfig in recognition.proto
@file:Suppress(
  "DEPRECATION",
  "RUNTIME_ANNOTATION_NOT_SUPPORTED",
)

package com.augmentalis.universalrpc.recognition

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Float
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.Suppress
import okio.ByteString

/**
 * Recognition configuration
 */
public class RecognitionConfig(
  /**
   * Target language code
   */
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 0,
  )
  public val language: String = "",
  /**
   * "google", "vivoka", "whisper"
   */
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "recognizerType",
    schemaIndex = 1,
  )
  public val recognizer_type: String = "",
  /**
   * Continuous listening mode
   */
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#BOOL",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 2,
  )
  public val continuous: Boolean = false,
  /**
   * Silence detection threshold
   */
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#FLOAT",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "silenceThreshold",
    schemaIndex = 3,
  )
  public val silence_threshold: Float = 0f,
  /**
   * Maximum recording duration
   */
  @field:WireField(
    tag = 5,
    adapter = "com.squareup.wire.ProtoAdapter#INT32",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "maxDurationMs",
    schemaIndex = 4,
  )
  public val max_duration_ms: Int = 0,
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<RecognitionConfig, Nothing>(ADAPTER, unknownFields) {
  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN,
  )
  override fun newBuilder(): Nothing = throw
      AssertionError("Builders are deprecated and only available in a javaInterop build; see https://square.github.io/wire/wire_compiler/#kotlin")

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is RecognitionConfig) return false
    if (unknownFields != other.unknownFields) return false
    if (language != other.language) return false
    if (recognizer_type != other.recognizer_type) return false
    if (continuous != other.continuous) return false
    if (silence_threshold != other.silence_threshold) return false
    if (max_duration_ms != other.max_duration_ms) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + language.hashCode()
      result = result * 37 + recognizer_type.hashCode()
      result = result * 37 + continuous.hashCode()
      result = result * 37 + silence_threshold.hashCode()
      result = result * 37 + max_duration_ms.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """language=${sanitize(language)}"""
    result += """recognizer_type=${sanitize(recognizer_type)}"""
    result += """continuous=$continuous"""
    result += """silence_threshold=$silence_threshold"""
    result += """max_duration_ms=$max_duration_ms"""
    return result.joinToString(prefix = "RecognitionConfig{", separator = ", ", postfix = "}")
  }

  public fun copy(
    language: String = this.language,
    recognizer_type: String = this.recognizer_type,
    continuous: Boolean = this.continuous,
    silence_threshold: Float = this.silence_threshold,
    max_duration_ms: Int = this.max_duration_ms,
    unknownFields: ByteString = this.unknownFields,
  ): RecognitionConfig = RecognitionConfig(language, recognizer_type, continuous, silence_threshold,
      max_duration_ms, unknownFields)

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<RecognitionConfig> = object : ProtoAdapter<RecognitionConfig>(
      FieldEncoding.LENGTH_DELIMITED, 
      RecognitionConfig::class, 
      "type.googleapis.com/com.augmentalis.universalrpc.recognition.RecognitionConfig", 
      PROTO_3, 
      null, 
      "recognition.proto"
    ) {
      override fun encodedSize(`value`: RecognitionConfig): Int {
        var size = value.unknownFields.size
        if (value.language != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(1, value.language)
        }
        if (value.recognizer_type != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(2, value.recognizer_type)
        }
        if (value.continuous != false) {
          size += ProtoAdapter.BOOL.encodedSizeWithTag(3, value.continuous)
        }
        if (!value.silence_threshold.equals(0f)) {
          size += ProtoAdapter.FLOAT.encodedSizeWithTag(4, value.silence_threshold)
        }
        if (value.max_duration_ms != 0) {
          size += ProtoAdapter.INT32.encodedSizeWithTag(5, value.max_duration_ms)
        }
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: RecognitionConfig) {
        if (value.language != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 1, value.language)
        }
        if (value.recognizer_type != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 2, value.recognizer_type)
        }
        if (value.continuous != false) {
          ProtoAdapter.BOOL.encodeWithTag(writer, 3, value.continuous)
        }
        if (!value.silence_threshold.equals(0f)) {
          ProtoAdapter.FLOAT.encodeWithTag(writer, 4, value.silence_threshold)
        }
        if (value.max_duration_ms != 0) {
          ProtoAdapter.INT32.encodeWithTag(writer, 5, value.max_duration_ms)
        }
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: RecognitionConfig) {
        writer.writeBytes(value.unknownFields)
        if (value.max_duration_ms != 0) {
          ProtoAdapter.INT32.encodeWithTag(writer, 5, value.max_duration_ms)
        }
        if (!value.silence_threshold.equals(0f)) {
          ProtoAdapter.FLOAT.encodeWithTag(writer, 4, value.silence_threshold)
        }
        if (value.continuous != false) {
          ProtoAdapter.BOOL.encodeWithTag(writer, 3, value.continuous)
        }
        if (value.recognizer_type != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 2, value.recognizer_type)
        }
        if (value.language != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 1, value.language)
        }
      }

      override fun decode(reader: ProtoReader): RecognitionConfig {
        var language: String = ""
        var recognizer_type: String = ""
        var continuous: Boolean = false
        var silence_threshold: Float = 0f
        var max_duration_ms: Int = 0
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> language = ProtoAdapter.STRING.decode(reader)
            2 -> recognizer_type = ProtoAdapter.STRING.decode(reader)
            3 -> continuous = ProtoAdapter.BOOL.decode(reader)
            4 -> silence_threshold = ProtoAdapter.FLOAT.decode(reader)
            5 -> max_duration_ms = ProtoAdapter.INT32.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return RecognitionConfig(
          language = language,
          recognizer_type = recognizer_type,
          continuous = continuous,
          silence_threshold = silence_threshold,
          max_duration_ms = max_duration_ms,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: RecognitionConfig): RecognitionConfig = value.copy(
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
