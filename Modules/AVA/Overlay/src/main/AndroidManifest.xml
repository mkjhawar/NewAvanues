<?xml version="1.0" encoding="utf-8"?>
<!-- filename: features/overlay/src/main/AndroidManifest.xml
     created: 2025-11-01 22:30:00 -0700
     author: Manoj Jhawar
     © Augmentalis Inc, Intelligent Devices LLC
     TCR: Phase 1 - Core Infrastructure
     agent: Engineer | mode: ACT -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Overlay permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>

    <!-- Context engine permissions -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        android:maxSdkVersion="34"
        tools:ignore="ProtectedPermissions"/>

    <queries>
        <!-- Query all apps for context detection -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application>
        <!-- Overlay foreground service -->
        <service
            android:name=".service.OverlayService"
            android:exported="false"
            android:foregroundServiceType="microphone"
            android:enabled="true" />

        <!-- Permission request activity -->
        <activity
            android:name=".service.OverlayPermissionActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar"
            android:launchMode="singleTask" />
    </application>

</manifest>
