# IDEACODE v9.0 - {PROJECT_NAME} Monorepo

## ⛔ STOP - BEFORE ANY FILE OPERATION

```bash
1. Read: FOLDER-REGISTRY.md (MANDATORY - check before creating folders)
2. Read: PROJECT-INSTRUCTIONS.md (app/platform-specific instructions)
3. Run: ideacode_validate_folder "{folder_path}" (validate before creation)
4. Check: Naming conventions (kebab-case, no type prefixes)
```

## 📋 MONOREPO REGISTRY SYSTEM

**CRITICAL FILES (Read at session start):**
- `FOLDER-REGISTRY.md` - Master registry of ALL folders (MANDATORY)
- `PROJECT-INSTRUCTIONS.md` - Centralized app/platform instructions

**RULES:**
1. ✅ ALWAYS check FOLDER-REGISTRY.md before creating folders
2. ❌ NEVER create folder variations (e.g., `voice-recognition/` vs `voice_recognition/`)
3. ✅ ALWAYS use kebab-case for folders (lowercase-with-dashes)
4. ❌ NEVER use type prefixes (use `authentication/` NOT `feature-authentication/`)
5. ✅ ALWAYS update FOLDER-REGISTRY.md after creating new folders

**MCP COMMANDS:**
- `ideacode_validate_folder "{path}"` - Validate folder before creation
- `ideacode_create_folder "{path}"` - Create folder with validation
- `ideacode_check_naming "{name}"` - Check naming convention
- `ideacode_registry_search "{query}"` - Search registry

---

## 🚨 HARD RULES

| Rule | Requirement |
|------|-------------|
| **MCP Tools** | Always use `ideacode_*` MCP tools for IDEACODE ops |
| **File Names** | `lowercase-kebab-case.md` (no `Feature-`, `Data-` prefixes) |
| **Branches** | Never commit to main. Use `feature/`, `bugfix/`, `hotfix/` |
| **No Copy-Paste** | Use library imports, never duplicate code |
| **No AI Attribution** | No "Generated by Claude", no emojis in commits |
| **Test Coverage** | 90%+ on critical paths |

### ⚡ YOLO MODE SAFEGUARDS (MANDATORY)

| Rule | Requirement |
|------|-------------|
| **No Shortcuts** | Never skip steps, use hacks, or cut corners for speed |
| **No Disable Without Re-enable** | If you disable anything (validation, feature flag, check), you MUST re-enable it |
| **100% Function Equivalence** | Refactoring MUST preserve ALL functionality - zero behavior changes |

---

## ⚡ Commands (v9.0)

```bash
/develop .yolo .swarm "feature"    # Full workflow
/fix .yolo .swarm "bug"            # Bug fix
/wiz .tutor                        # Interactive guided
/specify "feature"                 # Create spec
/plan spec.md                      # Create plan
/implement tasks.md                # Execute
/analyze .swarm                    # Code analysis
/think "question"                  # Deep reasoning
```

**Modifiers:** `.yolo` `.swarm` `.cot` `.tot` `.tutor` `.tcr` `.stop`

---

## 📁 Folder Structure

```
{PROJECT_NAME}/
├── FOLDER-REGISTRY.md      # Master folder registry
├── PROJECT-INSTRUCTIONS.md # App/platform instructions
├── android/                # Android platform
│   └── {apps}/
├── ios/                    # iOS platform
│   └── {apps}/
├── desktop/                # Desktop platform
│   └── {apps}/
├── common/                 # KMP shared modules (NO /libs!)
│   └── {modules}/
├── docs/
│   ├── {apps}/{Master,Platform}/
│   ├── ideacode/
│   ├── migration/
│   └── archive/{platform}/
├── .ideacode/
└── .claude/
```

---

## 📁 File Naming

```
lowercase-kebab-case.md           ✅ Good
Feature-Auth.md                   ❌ Bad (no type prefix)
LD-module-readme.md               ✅ Living doc
module-spec-feature.md            ✅ Module file
*-YYYYMMDDHHMM.md                 ✅ Timestamped
```

**Exceptions (UPPERCASE):** README.md, CHANGELOG.md, CLAUDE.md, LICENSE, FOLDER-REGISTRY.md, PROJECT-INSTRUCTIONS.md

---

## 🔧 MCP Tools

| Task | Tool |
|------|------|
| Spec | `ideacode_specify` |
| Plan | `ideacode_plan` |
| Implement | `ideacode_implement` |
| Test | `ideacode_test` |
| Commit | `ideacode_commit` |
| Standards | `ideacode_standards` |
| **Validate Folder** | `ideacode_validate_folder` |
| **Create Folder** | `ideacode_create_folder` |
| **Check Naming** | `ideacode_check_naming` |
| **Search Registry** | `ideacode_registry_search` |
| **Update Registry** | `ideacode_registry_update` |

---

## 📚 Key Locations

```
/Volumes/M-Drive/Coding/ideacode/
├── programming-standards/    # Code standards
├── protocols/               # Workflow protocols
├── .claude/commands/        # Slash commands
└── ideacode-mcp/           # MCP server
```

---

## 🎯 Quality Gates

| Metric | Target |
|--------|--------|
| Test Coverage | 90%+ |
| Blockers | 0 |
| Warnings | 0 |
| Folder Compliance | 100% |
| Naming Violations | 0 |

---

## 📖 Full Docs

- **Modifiers:** `/modifiers`
- **Help:** `/help`
- **Standards:** `ideacode_standards --context "task"`
- **Folder Registry:** `FOLDER-REGISTRY.md`
- **Project Instructions:** `PROJECT-INSTRUCTIONS.md`

---

**Author:** {AUTHOR} | **License:** Proprietary | **Version:** 9.0 | **Updated:** {DATE}
