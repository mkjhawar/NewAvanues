<?xml version="1.0" encoding="utf-8"?>
<!--
  Avanues - Consolidated Modular Platform
  Three launcher icons: Avanues (hub) + VoiceAvanue (voice) + WebAvanue (browser)
  Copyright (C) Manoj Jhawar/Aman Jhawar, Intelligent Devices LLC
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet for web browsing and AI features -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Microphone for voice recognition -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- System overlay for cursor, HUD, and overlays -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Foreground service for voice recognition and cursor -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Notifications for service status (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibration for feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Boot receiver for auto-start (optional) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Camera for eye tracking and WebXR (optional) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Storage for downloads (scoped storage used) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />

    <!-- Optional hardware features -->
    <uses-feature android:name="android.hardware.microphone" android:required="true" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".VoiceAvanueApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Avanues"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceAvanue"
        android:hardwareAccelerated="true"
        tools:targetApi="34">

        <!-- Disable Sentry auto-init (transitive from WebAvanue).
             Call SentryManager.init() manually when DSN is configured. -->
        <meta-data android:name="io.sentry.auto-init" android:value="false" />

        <!-- Main Activity (no direct LAUNCHER — routed via aliases) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Avanues"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboard|keyboardHidden"
            android:theme="@style/Theme.VoiceAvanue.SplashScreen"
            android:windowSoftInputMode="adjustResize">

            <!-- Deep links for browser URLs -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- Launcher 1: Avanues (master hub dashboard) -->
        <activity-alias
            android:name=".AvanuesAlias"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_avanues"
            android:label="Avanues"
            android:roundIcon="@mipmap/ic_launcher_avanues_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="launch_mode" android:value="hub" />
        </activity-alias>

        <!-- Launcher 2: VoiceAvanue (voice/accessibility dashboard) -->
        <activity-alias
            android:name=".VoiceAvanueAlias"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_voice"
            android:label="VoiceAvanue"
            android:roundIcon="@mipmap/ic_launcher_voice_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="launch_mode" android:value="voice" />
        </activity-alias>

        <!-- Launcher 3: WebAvanue (full browser) -->
        <activity-alias
            android:name=".WebAvanueAlias"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_web"
            android:label="WebAvanue"
            android:roundIcon="@mipmap/ic_launcher_web_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="launch_mode" android:value="browser" />
        </activity-alias>

        <!-- Accessibility Service for VoiceOS -->
        <service
            android:name=".service.VoiceAvanueAccessibilityService"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">

            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Command Overlay Service (numbered badges for voice selection) -->
        <service
            android:name=".service.CommandOverlayService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="accessibility_overlay" />
        </service>

        <!-- Cursor Overlay Service -->
        <service
            android:name="com.augmentalis.voicecursor.overlay.CursorOverlayService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="accessibility_cursor" />
        </service>

        <!-- Voice Recognition Service -->
        <service
            android:name=".service.VoiceRecognitionService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <!-- RPC Server Service (dormant — started on demand via Settings) -->
        <service
            android:name=".service.RpcServerService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="ipc_server" />
        </service>

        <!-- Boot Receiver -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
