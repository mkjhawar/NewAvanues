package com.augmentalis.avaelements.core.runtime

import com.augmentalis.avaelements.core.*

/**
 * Test implementations for expect/actual functions
 *
 * These are stub implementations that should not be called during testing.
 * Tests use TestPluginSource which bypasses the actual plugin loading.
 */
