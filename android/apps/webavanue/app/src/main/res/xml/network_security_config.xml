<?xml version="1.0" encoding="utf-8"?>
<!--
SECURITY FIX: Network Security Configuration (CWE-295)
Enforces HTTPS for all connections by default.
Cleartext (HTTP) traffic is blocked except for localhost during development.
-->
<network-security-config>
    <!-- Base configuration: Block cleartext traffic by default -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug configuration: Allow cleartext for localhost only -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Allow localhost for development/testing -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
</network-security-config>
