<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- Speech Recognition Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Voice Recognition Service Permission -->
    <permission
        android:name="com.augmentalis.voicerecognition.BIND_SERVICE"
        android:protectionLevel="signature" />
    
    <uses-permission android:name="com.augmentalis.voicerecognition.BIND_SERVICE" />
    
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.VoiceRecognition"
        android:icon="@mipmap/ic_launcher">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.VoiceRecognition"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Voice Recognition Service -->
        <service
            android:name=".service.VoiceRecognitionService"
            android:exported="true"
            android:enabled="true"
            android:permission="com.augmentalis.voicerecognition.BIND_SERVICE">
            <intent-filter>
                <action android:name="com.augmentalis.voicerecognition.RECOGNITION_SERVICE" />
            </intent-filter>
        </service>
        
    </application>
</manifest>